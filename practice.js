class User {
  constructor(email, password) {
    this.email = email // this objects email = the email that was passed in
    this.password = password // this objects password = the password that was passed in
  }
}
